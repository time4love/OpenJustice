# docs/ — the index

**How to read this folder.** `CLAUDE.md` at the repo root first. Then the design that owns the
subject; then the plan that builds it. Findings are dated records, never edited, pointed at from the
plan's `STATUS:` lines. Everything before the five designs is a pre-design plan: nothing in one is a
required task by virtue of being listed — its open items are triaged in the dated findings doc named
below. This index carries pointers, never content; a test holds that every markdown file under `docs/`
is reachable from here and that everything in the archive says where its subject lives now.

## The five target designs — signed off, in dependency order

- [gf-interaction-flows.md](gf-interaction-flows.md) — the corpus: survey, walk, the stops for judgement, the marking page
- [gf-evidence-flows.md](gf-evidence-flows.md) — evidence: a promoted corpus record, its versions, the return path
- [gf-thesis-flows.md](gf-thesis-flows.md) — the thesis: framing, versions, arguments, the gate, the public page
- [gf-document-flows.md](gf-document-flows.md) — documents: the two doors, sealed intake, custody, citation
- [gf-ui-flows.md](gf-ui-flows.md) — the browser: dialogs by link, the researcher's gated read view, the public surface; routes are tools' answers

## The approved UI boards — the image a page is built and graded against

- [boards/gf-ui-boards-2026-09-22.html](boards/gf-ui-boards-2026-09-22.html) — the 09-21 image unchanged, plus section י, THE DOCUMENT DOOR, drawn from the design text and the marking page as PROPOSALS and ruled at the page 2026-09-22: י1 and י1ב the upload DIALOG at 390 (one file; every fact — the TITLE, the page, the date, derived-from — settled in the conversation, carried by the link, drawn as labels; the command to paste), י2 the same at 1440 WITHOUT the shell (ruled for both dialogs), י3 the DOCUMENTS lens on `/research/corpus` (ruled: in R74), י4 the `#doc_` chip as the document glyph plus the title's first words, ד2·י the working view with a document cited. **The approved CURRENT image only**; „טיוטה" marks a word not yet in the catalogue.
- [boards/gf-ui-boards-2026-09-21.html](boards/gf-ui-boards-2026-09-21.html) — SUPERSEDED 2026-09-22 by the file above, banner in place; UI-8's nine approved boards, drawn from the real staging bodies: ב `/research` · ד2 · ג2 · ג3 the working view (אפשרות ב) · ה `/research/corpus` · ו the extraction sheet · ז the claims lens · ח the framing sheet · ט·ב the single-page view. **It holds the approved CURRENT image only** — cancelled options and before-states are not drawn, because a file that mixes them is a file a seat can grade against the wrong one. Regenerate with `python3 boards.py` beside it (`bodies.json` are the real bodies); an update is a NEW dated file, and this one then takes a superseded banner and moves below.

## Reasoning, as-built, and the route between them

- [gf-architecture-target.md](gf-architecture-target.md) — why the designs are shaped as they are; §9–§11 defer to the flows
- [gf-architecture-current.md](gf-architecture-current.md) — the code as it stands, 2026-09-02; the refactor's "from"
- [gf-refactor-plan.md](gf-refactor-plan.md) — corpus steps 0–10, evidence steps 11–16; the test rules in §4
- [gf-thesis-refactor-plan.md](gf-thesis-refactor-plan.md) — thesis steps 17–26; the document plan chains after it
- [gf-document-refactor-plan.md](gf-document-refactor-plan.md) — document steps 27–37, on clean ground; the seams to the three plans before it, and the switch
- [gf-ui-refactor-plan.md](gf-ui-refactor-plan.md) — the browser, steps UI-1 to UI-10, before the document plan: the harness first, the routes before the pages, one cut-over at the end; every instrument of the UI design's A5 given a file and a step
- [gf-factual-layer-rebuild-dev-plan.md](gf-factual-layer-rebuild-dev-plan.md) — the levels; `grep -n '^\*\*STATUS:'` is the level authority
- [gf-researcher-day.md](gf-researcher-day.md) — the flows read as one working day, tool by tool
- [gf-prosecutor-dev-plan.md](gf-prosecutor-dev-plan.md) — the Prosecutor design, re-read under thesis flows §10; deliberately not next
- [gf-two-session-protocol.md](gf-two-session-protocol.md) — how a step is handed from one DEV + REVIEW pair to the next: the three handoff files, what each must carry, and the checker that says what is missing

## Pre-design plans still in force — each carries a head note pointing at the triage

- [gf-mcp-oauth-dev-plan.md](gf-mcp-oauth-dev-plan.md) — OAuth 2.1, the live auth model; the authority on auth and on bootstrapping a researcher
- [gf-cost-exposure-dev-plan.md](gf-cost-exposure-dev-plan.md) — rate limiting, live; its route inventory is retired by the designs
- [gf-adverse-event-report-schema-dev-plan.md](gf-adverse-event-report-schema-dev-plan.md) — the public self-reports feature, live in production
- [gf-reporter-legal-pass.md](gf-reporter-legal-pass.md) — the reports feature's legal pass; two items still want counsel
- [gf-production-readiness-prerequisites.md](gf-production-readiness-prerequisites.md) — what still gates production; items closed by the triage say so
- [gf-trajectory-state-implementation.md](gf-trajectory-state-implementation.md) — why `ClaimTrajectory` is shaped as it is
- [gf-verification-tools-dev-plan.md](gf-verification-tools-dev-plan.md) — `verify_claim_text`, `audit_thesis_claims`, `check_publication_readiness`
- [gf-chat-tutorial-dev-plan.md](gf-chat-tutorial-dev-plan.md) — the in-chat tutorial, the approach that won; its chapter queue is in the triage

## User-facing guides

- [gf-chatgpt-mcp-connector-guide.md](gf-chatgpt-mcp-connector-guide.md) — connecting ChatGPT to the MCP server, in Hebrew

## Findings, by date — newest first, never edited

- [gf-document-production-chain-read-2026-09-23.md](gf-document-production-chain-read-2026-09-23.md) — PRODUCTION's reads before the first SHIP of the document layer, each in the production container with the environment stated twice: the CHAIN ROTATION read PASSES — registry `0xDE42…9823`, `totalEvidence()` 1, index 0 `DOCUMENT_SHA256` — and names a stale fact, CLAUDE.md's `0x0e21…0d22`; the column drop NOT SIMULATED because production has no `DocumentContentVersion` until step 28's migration creates it in the same deploy, so it can reach no row; and production's `storage.buckets` counted with a control that SEES staging's bucket — production holds NONE, so the migration creates `documents` private and the legacy public `evidence` bucket is staging's alone
- [gf-document-step-27-chain-read-2026-09-23.md](gf-document-step-27-chain-read-2026-09-23.md) — document step 27's CHAIN-ROTATION READ on STAGING, taken three steps after the plan asked for it and PASSING on all three legs: the deployed service is configured with the rotated-to registry `0xDA3B…4C73`, a contract of 2,799 bytes exists at it, `totalEvidence()` is 27 and index 0 carries `DOCUMENT_SHA256` — which is `ANCHOR_SCHEME`'s VALUE, the distinction that made the seat's own first run report a false failure — so `WRITES_ALLOWED` is true; taken TWICE by different paths, a public-RPC read from a laptop that answers what is AT an address and an in-container run that answers which address the service will WRITE to, agreeing on address, total, index 0's category and its fileHash, with all four axes of `assertOperationalContext` concurring; the rotated-FROM registry read for contrast, where index 0 carries the retired classifier-label formula and `WRITES_ALLOWED` is FALSE — correcting the seat's earlier claim that the guard could not tell the two apart, and narrowing the reason the read is needed to the one that holds: the guard sees only the registry it is pointed at, never whether the configuration points at the right one; and what the read does NOT establish — production, whose read is taken at `SHIP`
- [gf-document-step-30-2026-09-23.md](gf-document-step-30-2026-09-23.md) — document step 30, the researcher's door BUILT in four reviewed chunks, each ended at a page the researcher approved: the bucket migration's APPROVAL RECORD — the approved text's sha `451c442f…66c2`, the landed file's `491042fa…ad91`, and the diff between them (two header comments removed, one comment corrected, the SQL byte-identical), recovered from the seat's own write of the approved text; the rulings the step was built to, each pointing at its design line; what the review caught — a transaction double blind at three sites, EQUALS_CAPTURE comparing spellings, copy that did not match the code, a dialog that hung on an unexpected answer, and the marking page the RESEARCHER found could not scroll at the page, 679 px unreachable while every jsdom case was green; the measurements at the build's close; and what is OWED — `LAND`'s database reads, the staging exercise that closes the step, production's reads at `SHIP`, and step 31's payment of every commitment
- [gf-document-step-30-staging-exercise-2026-09-23.md](gf-document-step-30-staging-exercise-2026-09-23.md) — document step 30 CLOSED ON THE REDUCED SET, the SUCCESSOR of the build record's OWED paragraph: the staging exercise as run (16 calls, the real XLSX and the paper uploaded from a browser, one paid draw), plan :184–:189 graded cold (MET · MET-AT-THE-TOOL · OWED, by call and field), the lens against board י3, the two container instruments; findings F1 (the text inline blows the client's result cap), F2 (the dialog cannot send bytes already stored), F3 (a cut paid answer stored as an opinion), each with its code and REVIEW's own part in missing it; the researcher's RULINGS verbatim and the three at the close (the plan's STATUS convention, the text's place in `read_document`, `INCOMPLETE_ANSWER`); the gate `gate:document-30`, #578–#581
- [gf-document-step-30-second-send-2026-09-24.md](gf-document-step-30-second-send-2026-09-24.md) — the deferral of step 30's close MET: the second send of the dataset REDONE on staging's page after #585 („כבר במחסן · ממתין לפקודה", `existed: true`, three arrivals read back, every field before the capped text arriving), the signed `textUrl` measured (112,602 characters, its hash the version's; unsigned 401), and the 80,000-character cap measured to fit the client at 85,822 JSON characters
- [gf-document-step-31-staging-pass-2026-09-24.md](gf-document-step-31-staging-pass-2026-09-24.md) — document step 31 MET ON STAGING (PR #588): step 30’s two documents owed (exit 2), the standing pass anchoring both (registry indices 27, 28), exit 0 after, `check_on_chain_status` ATTRIBUTED under `DOCUMENT_COMMITMENT`, `anchors-explainable` 29 entries explained; a new document (the Nuremberg Code) anchored AT RECEIPT through MCP (index 29); the R80 rulings the step was built on; the production pass HELD.
- [gf-document-step-33-staging-exercise-2026-09-25.md](gf-document-step-33-staging-exercise-2026-09-25.md) — document step 33 CLOSED ON THE REDUCED SET on staging: the researcher's `#doc_` draft re-pinned to the re-derived Nuremberg Code, debated (SUPPORTS, the quote PRESENT by the verdict rule), promoted as the FIRST DOCUMENT Evidence row with NO chain write (the registry entry byte-identical, the total 30 before and after), `decide_gap CITED`; several pages plus a document DEFERRED to the real end-to-end run, the sealed items and the arrival to step 32; found: connector schemas cached per conversation, the re-pin not drawn in the transcript, an unpromoted document's moved pin not shown before step 34
- [gf-document-step-34-publication-2026-09-26.md](gf-document-step-34-publication-2026-09-26.md) — document step 34, SKELETON (written at chunk 6, before the staging exercise): what each chunk built and where it landed, the rulings it was built to by the design line that carries each (Q-D, Q-E, Q-G, Q-H, Q-R1, the conformed :1523, Q-S, Q-R2), and the staging exercise's acts left EMPTY for the seat that runs it
- [gf-document-reader-join-2026-09-25.md](gf-document-reader-join-2026-09-25.md) — the PDF reader's join FIXED and re-measured (the Hebrew doc's ruling 3): `v2`, a geometry join at 0.06 font sizes (RULED 2026-09-25); on MK 05/2023 the three quotes PRESENT exactly and split words 414 → 12, one line per drawn line so `verify_claim_text`'s context is ~400 characters, not a page; the English control NOT identical by one comma (reported, not tuned); §2b's fifteen "visual-order" negative gaps corrected to touching boxes, while the visual-order residual SURVIVES `v2` unchanged („mIU/ml 10” ×22 in both); a REGRESSION at 12 identified word gaps drawn with no space („10mIU/ml”); the re-derivation window on staging and at SHIP
- [gf-document-hebrew-text-layer-2026-09-24.md](gf-document-hebrew-text-layer-2026-09-24.md) — the Hebrew text layer measured before the first Hebrew citation: 0 of 3 quotes PRESENT, because the PDF reader splits words and detaches punctuation (`documentExtractor.ts:225` joins every item with a space); RULED 2026-09-25: a geometry join at a new `CURRENT_EXTRACTOR` before step 34's sketch, the verdict rule and check 19 unchanged, the quote object not adopted (an issue)
- [gf-document-step-29-rounds-3-4-2026-09-23.md](gf-document-step-29-rounds-3-4-2026-09-23.md) — document step 29b, ROUNDS 3 AND 4, where the step closes: the RE-DERIVATION TRAP ruled and closed — `derivedUnder`, an append-only list of every extractor version that REPRODUCED a text, with `CURRENT(d)` reading MEMBERSHIP of it and `extractorVersion` never overwritten, because two correct clauses (§3 :317 forbidding a second row, A3 :1368 asking for equality) had an EMPTY INTERSECTION that left a reproduced document permanently uncitable under a hard check; its migration, hand-written and then checked against the generator OFFLINE, which caught a DEFAULT the datamodel does not declare; `readFailed` and the FIVE outcomes, where a corrupt file of an accepted type is held as bytes-only and never refused because A5's `UNREADABLE` is a key that does not open a ciphertext; the EMPTY-CSV defect the seat found in its OWN round-2 code, where the serialiser's header stood in as the document's content; FOUR measurements on loading an ESM-only `pdfjs-dist` under jest, including `createRequire` NOT escaping jest's process-wide `Module._load` hook and `--experimental-vm-modules` silently stopping 93 of 3199 gating cases from running; the PDF reader therefore held by a PROCESS-LEVEL test over `dist/` in the gating project, whose vacuity guard is the compiled and source `CURRENT_EXTRACTOR` agreeing rather than an mtime; the RETIRED PASTE still GREEN in the acceptance suite and deleted with its hand-kept map, leaving ONE spelling of the fixture set; and the two controls that caught the seat itself — a lint ratchet on a type lying about a closure-assigned flag, and a decoy that reddened NOTHING and so found an unasserted seam
- [gf-document-step-29-round-2-2026-09-23.md](gf-document-step-29-round-2-2026-09-23.md) — document step 29b, ROUND 2, the round that answered the cold grading of round 1 (`handoffs/R75-review-state.md`: 2 HIGH, 6 MEDIUM, 4 LOW): the researcher's rename of `recomputable` to `recomputableDocument`, which let both edited sibling acceptance suites be restored byte-for-byte; `text/plain` RULED bytes-only with its two citations and no code; the reader table answering for every type it accepts, CSV read through its own arm with cells kept as RAW STRINGS because exceljs's default map parses a date in the HOST'S TIMEZONE (measured: the same bytes, two texts, two machines) and the legacy .xls type removed; the fourth fixture made an AUDIO file so the set reaches FOUR DISTINCT reason codes where two kinds were previously indistinguishable; `CURRENT_EXTRACTOR` narrowed off `| null` with its two dead branches, two dead format strings and five stale comment blocks; the version string PROPOSED as `…-exceljs4.4.0-xlsxcells-csvraw-nfc` with a guard tying both readers to what package.json declares, and the dependency choice itself recorded as an OPEN DIFFERENCE between two briefs; 471 lines of untested service code given four test files and fifteen decoys, TWO of which caught a weak assertion of the author's rather than the instrument; and the new gap named rather than implied — NO test in any project can load `pdfjs-dist` 6.3.289, which is ESM-only, so the PDF reader is exercised by `extractor-coverage` alone
- [gf-document-step-29-2026-09-23.md](gf-document-step-29-2026-09-23.md) — document step 29, the extractor BUILT to the ruling of the same day: `CURRENT_EXTRACTOR` names every stage (`v1-pdfjs6.3.289-streamorder-ocr-none-xlsxcells-nfc`), a text-layer PDF and a spreadsheet are COMPUTED and a scan is bytes-only; the FOUR counts measured 2 COMPUTED / 2 BYTES-ONLY and the text identical across three runs per kind; the four SYNTHETIC fixtures with a generator of Node builtins alone and a guard that regenerates them byte for byte; `DocumentContentVersion`'s one writer deriving OUTSIDE the transaction, the derivation pass that writes versions and never a decision, and the OPINION register refusing an unlabelled reading; the candidate measurement that disqualified `node-xlsx` on a CDN tarball URL; and four findings — `recomputable` naming two different predicates in two appendices, a fifth suite case no correct implementation could satisfy, the paste cases the 2026-09-23 ruling retires in step 30's file, and two source pointers naming a step that has landed
- [gf-extractor-ruling-2026-09-23.md](gf-extractor-ruling-2026-09-23.md) — the extractor RULED: `ocr-none` ships, and a SCANNED DOCUMENT IS OPENED TO BYTES. A PDF with a text layer is read and its text is COMPUTED; a scan is bytes-only and a quoted span from one is UNCHECKED, with the reason. The trade, which is not checked-versus-unchecked: a weak OCR turns an honest "not machine-checked" into a false blocking ABSENT against the platform's own researcher, and `extractor-coverage` cannot see it because it counts whether text was derived, never whether it is right. Carries the FULL advisory analysis as its source — per-candidate Hebrew coverage and determinism, the rasteriser as an uncounted stage, the version string's grammar, and the two gates any future OCR must pass
- [gf-document-design-session-2026-09-22.md](gf-document-design-session-2026-09-22.md) — the document design RE-VALIDATED against three record kinds (official PDFs and a dataset, a whistleblower's file, a video interview) and SIX RULINGS amended in place at zero line delta: the researcher's door is an UPLOAD DIALOG under the dialog rule, because claude.ai cannot hand a file to an MCP tool (checked against the connector, MCP and Files API docs); one PRIVATE BUCKET per environment keyed by DOC_ID, cache or held by whether a row exists; a spreadsheet is COMPUTED text; video is media plus a derived transcript with speech-to-text as OPINION; step 31 in the round on staging with production held; the round scoped from the step bodies — 27 · 28 · 29 · 30 · 33 · 31 · 34, the public door later
- [gf-ui-step-8-2026-09-22.md](gf-ui-step-8-2026-09-22.md) — UI step 8, THE READ VIEW, closed on the REDUCED set: fourteen landings (#544 to #566) closing on `bca6d2b` — `/research`'s four regions, the gated chronology, the working view's read and context block, the transcript and its door, and THE CENTRE (the tick line and the text, UI-5's components CALLED against board ד2); the LOADER rebuilt inside this step by the researcher's ruling, with its cost record beside it; the HISTORY SIZE finally TAKEN (37 turns, 6,768 px, 9.08 screens at 375, identical local and staging) so no cursor is added; NO LIBRARY chosen and the step recording that it chose none; the exercise on the real staging body WITH ITS RAW SOURCE KEPT, the centre 1,472 px and the page 3.23 screens with no horizontal scroll and no disclaimer rendered; the disclaimer FALSE ALARM, where a hand-written probe read the sentence out of Next.js's RSC payload inside a `<script>` and the landed instrument was right all along; the PARSER that had narrowed a citation to four fields since R70 with nothing able to fail on it — "a parser that narrows looks identical to a parser that validates"; the three deferrals with their design references; and two things recorded and left to the researcher at the page — the tick line's 1 + 2 wrap at 375 px, and the board's tick order disagreeing with the page's because the generator builds from `DEBATE_OPENED` turns
- [gf-thesis-read-cost-2026-09-22.md](gf-thesis-read-cost-2026-09-22.md) — why the working view would not load: 39 delegate calls, the body assembled three times, and a ~550 ms per-query floor from a laptop. What was fixed, six corrections a cold read made, what needs no amendment, and the target shape (one loader, narrowed record resolution, `owed` on the envelope) with its two open rulings
- [gf-ui-step-7-2026-09-20.md](gf-ui-step-7-2026-09-20.md) — UI step 7, THE CHRONOLOGY, THE PAGES LIST, THE LENSES, THE THREE RECORD PAGES AND THE CLAIMS VIEW: ten landings, #522 to #542, closing on `f09c909`; the corpus types and the two pure libraries, the theses list with the CI type-check gate, the third voice's container, the pages list from the public facet alone, the stream under the page card's true-time strip (captures as dots, cited ones ringed, diffs as midpoint bars by the square root of their chunk count, everything the gate hides still drawn lighter), the CITED lens, the capture page with the archive as second witness and the chain check pressed TRUE at index 22 on staging, the diff page through one `DiffRuns`, the records page with `notEvaluable` LIVE on 19 of 22 captures; THREE ENVELOPES the appendix had named only by their fields, written in place from the backend's own interfaces and one real body each before a parser could be wrong a fourth time; the CLAIMS view per page, where the read served flip SPANS and the sheet's diff links could not be composed until the stored per-capture vector rode out as `captures[]` — 87 of 87 flip pairs real, against 38 of 87 from the spans; the copy frozen by sha before it landed and still wrong twice on the live corpus (an empty capsule drawn where the platform meant silence; a plural noun after 1 on five rows of two); the decoys both seats planted and the seven that reddened NOTHING, each of which became a finding; the density measured (43 rows, 22 captures, May 2022 the densest); the chain check's press rate UNMEASURABLE from Railway's request log, which retains the current deployment only; and what stays open — a sheet eight phone screens tall, the reason sentences, the one 404 that names a thesis
- [gf-ui-step-5-2026-09-18.md](gf-ui-step-5-2026-09-18.md) — UI step 5, THE PUBLIC THESIS PAGE: the researcher’s MARKDOWN in every field of their voice and not only the published text (the live rationale was 2,632 characters over three blank-line breaks and reached the reader as ONE run-on block, because the renderer had a single caller), with the archive EXCLUDED because a `#` standing in a captured page would re-interpret what the record says; the PALETTE re-grounded on Claude’s own surface, SAMPLED not assumed — and the round’s lesson, that the paper carries the WARMTH while the muted carries the READABILITY, since every paper between the old value and the new moves the contrast by 0.23 and no element failed AA before the change; the ARCHIVE taken out of the researcher’s voice component and given its serif by declaration, the face unmoved and now cased; §:427 answered — markdown-it as a PARSER with `html: false` and `linkify: false`, twelve table token types, task lists and footnotes ruled out, an image rendering its alt text alone; three LAX probes and a decoy that reddened NOTHING and found a property `built-as-drawn` had never asserted; the three-voices READING TEST **PASSING** for the first time — „הקולות השונים ברורים" — though carried by TYPOGRAPHY and not by the component split §16 describes; three measurements that rotted in comments when the tokens moved (#512); and the HEADING taken out of the step by the researcher, as a specification question and not a display one
- [gf-ui-step-4b-2026-09-17.md](gf-ui-step-4b-2026-09-17.md) — UI step 4b, THE SHELL: the sidebar that is the nav with „הבית” retired as a concept, the right pane EMPTY and out of the DOM until a page declares a tab, ONE Sheet primitive closing R56's F1, the tokens and FOUR self-hosted faces (both Hebrew subsets carry ZERO digits, which is why the Latin ones landed) with `adjustFontFallback: false` because the generated Fallback family is Arial mid-chain (#500, `1dc1231`); the copy frozen by sha before it landed; seven instruments red first and twenty-one decoys across two seats, one refused as malformed; the five defects only a browser found — a collapsed sidebar that could not reopen, a drawer on the wrong edge under RTL, a splitter moving one step per burst, and on staging **F8, the serif self-hosted and applied to NOTHING**; the researcher's canvas reading „נראה טוב, שמור את הכחול והאנימציה” ruling the door's dark field into the design, and their reading test FAILING — „currently it is not clear” — diagnosed to three missing separations and a fourth voice absent by design
- [gf-ui-design-session-2026-09-16.md](gf-ui-design-session-2026-09-16.md) — the UX design session: the Claude-shaped three-pane shell ruled (sidebar of categories with last-watched lists, centre = the conversation, a tabbed right pane; two centres by URL), seven steps rendered on one design canvas from the live staging bodies and approved as images before any code; the folded preface, „צילום” not „לכידה”, the six call-page decisions settled, the eleven stream kinds with eight glyphs, the system sheet; the in-place amendments to the UI design and the re-brief of UI-5…UI-9 against the canvas pages
- [gf-ui-step-4-2026-09-15.md](gf-ui-step-4-2026-09-15.md) — UI step 4, the layout: the chrome mounted once in the locale layout with one nav list keyed by identity, the approved copy byte for byte, no disclaimer in the footer by ruling, the sixteen header imports removed and the chat widget and debug console unlinked (#490); `nav-is-the-map` and the locale-document cases observed red on their decoys; the staging exercise at 375 px predicted and scored, which found two defects the fixtures could not see — a locale switch leaving English right-to-left, fixed by making the locale layout the root (#491), and that move silently dropping the share image, restored against production's baseline (#492); the unbuilt nav entries' 404 known until UI-10
- [gf-ui-step-3-2026-09-15.md](gf-ui-step-3-2026-09-15.md) — UI step 3, the routes: twelve public and fourteen gated under one `requireResearcher` mount, one adapter over one status table, every per-page and gated tool split into a core both doors call with its contract unchanged, `/api/stats` and `/api/forensics` retired, the thesis body's `pages` carrying `trackedUrlId` (#488); #487 ruled GATED and the per-page search not mounted, the trajectories route over `list_trajectories`; every instrument observed red on its decoy, and two gaps the decoys found before landing (a page loaded twice, a write log read after it was emptied); the lint debt locked 84 → 82 from CI; the staging reads predicted on record and scored from a signed-in browser, byte for byte, no miss, the draft 404 unobservable
- [gf-ui-step-2-2026-09-15.md](gf-ui-step-2-2026-09-15.md) — UI step 2, the reads: `list_corpus`, `list_trajectories` and `search_corpus` over one loader with the per-page row builders moved out of two KEEP files by ruling, `scope: 'mine' | 'all'` on the two thesis lists with the default byte for byte, the surface 42 → 45 with approved copy (#484); the operational values named (`limit` 100, the order and its tie-break, days inclusive); the stored-register search ruled; the acceptance files observed red by name and every instrument red on its decoy, the two the fixture could not fire; the staging exercise predicted on record and scored, one miss (no diff carries evidence — the three promoted records are captures); no route and no page, which begin at UI-3
- [gf-ui-step-1-2026-09-15.md](gf-ui-step-1-2026-09-15.md) — UI step 1, the harness: the frontend's first jest project through next/jest, the source-scan and render-scan helpers, `name-never-glass-fortress` and `messages-parity` observed red on their decoys before green (3 suites / 18), a Server Component rendered through one next-intl stand-in over the real messages, a missing message made to throw, the ESM-only chain next/jest will not transform, the lockfile's 38 named moves including `prisma` corrected from a dev dependency, and the frontend job made a required check on `staging` (#481); no page rendered on staging, which begins at UI-4
- [gf-thesis-run-b-2026-09-15.md](gf-thesis-run-b-2026-09-15.md) — run B, the first real publication of a thesis on staging, UNSTEERED through the connector with every step predicted and checked by data: the claim re-framed to the three capture records the corpus can argue (the two diffs carry CONTRADICTED chunks), the absence settled in the raw archive, three debates and promotions with no chain write, the gaps decided and a FOIA request re-drafted, the analysis and readiness, the publication and its opened page, nothing owed, and `audit-theses` in the container over one published version exit 0 — the ledger's first recorded run; the findings by owner (state asserted without reading it, five times; a conversation that lost its history; #473); the critic's record-grounded challenges and the corpus to add, as material for step 26
- [gf-thesis-step-24-2026-09-15.md](gf-thesis-step-24-2026-09-15.md) — thesis step 24, after publication: REVIEWS and `list_thesis_reviews` (the envelope `{ owed, reviews }`, FLAGGED's material the pin beside CURRENT from one loader extracted from evidence's list) and `audit-theses` completed through one evaluation per published version (#448, surface 42, no migration); the audit run in the staging container over zero published versions, exit 0, and deliberately not recorded on the integrity board because a run over nothing is never proof; the author's list read through the connector unsteered on a one-line prompt, every prediction held and nothing written; three presentation findings; the plan's own exercise owed to the first real publication
- [gf-thesis-step-23-2026-09-14.md](gf-thesis-step-23-2026-09-14.md) — thesis step 23, the gate, publication and the public reads: the T5/T6 contract green (#446, surface 41, `ThesisAnalysis.researcherId` taken in the container) and the live run on staging 2026-09-14 UNSTEERED through the connector, predicted on record and verified by data — readiness free and then paid with the assessor's opinion labelled, every hard failure named with an unpromoted citation examining nothing, a publication refused with its attempt recorded and nothing moved, the public reads a 404 identical to a made-up id, a withdrawal of nothing refused; nothing published, by the researcher's choice; the findings: a stale pin invisible before promotion, the claim/text mismatch in a third artifact, the call page's public-interest statement (ruled: read from the thesis page)
- [gf-thesis-step-22-2026-09-14.md](gf-thesis-step-22-2026-09-14.md) — thesis step 22, analysis and gaps: the T4 contract green (#444, surface 38) and the live run on staging 2026-09-14 UNSTEERED through the connector, predicted on record — the critic drawn once with its fingerprint equal to the context read's, two gaps entered under the ids the critic computed, a FOIA request drafted with the platform's addresses and recorded, STALE_SEQUENCE provoked and refused with nothing written, the public call empty to an anonymous caller; ten of twelve predictions held; the findings: the assistant's own opinion unmarked, a paid act with no author (ruled: it records who spent it), tool naming to get its own session, the placeholders that stay
- [gf-thesis-step-20-2026-09-14.md](gf-thesis-step-20-2026-09-14.md) — thesis step 20, the version write: the T2 contract green (#440) and the owed staging exercise run 2026-09-14 UNSTEERED through the connector, predicted on record and verified by data — the FIRST THESIS on staging under `PATIENT_RIGHTS_13`, CLAIM_MISMATCH on one changed character of 689, both pins computed by the platform and matched to `list_findings`, STALE_HEAD against a moved head, the refused attempts absent from a derived history, the draft invisible to an anonymous `list_theses`; the exercise could not take its first step until two reads existed — `list_framings` and `list_pages` (#441) — the step-19 F3 closed; the findings by owner: the production connector chosen first by a fresh conversation (again), `list_findings` a generic name for a timeline, a session that paraphrases a verbatim claim, narrated returns, a token boundary the surface never states; step 21 closed by its plan note as already built
- [gf-rebuild-production-ship-and-walk-2026-09-13.md](gf-rebuild-production-ship-and-walk-2026-09-13.md) — refactor step 9 on PRODUCTION, the `SHIP` of head and the start of sub-step 6: `SHIP`'s five refusals read as data; head `c2c8933` (95 commits) merged and deployed, its four migrations applied on the emptied database and taken (71 in the ledger, 35 tables, drift clean), the handshake read; the researcher signed up and bootstrapped, the connector re-authorized, `get_environment` CONFIRMED; corona surveyed (133 · 95, `held` 0); the Gate 0 marking with the adverse-event questionnaire link deliberately left in the text; **index 0 written on `0xDE42…9823` on Base MAINNET, stamped `DOCUMENT_SHA256`, ATTRIBUTED, 0.00000097 ETH**; the walk stopped by the researcher after it — 132 rows UNFETCHED, resumable
- [gf-rebuild-production-drop-2026-09-13.md](gf-rebuild-production-drop-2026-09-13.md) — refactor step 9, sub-step 5 on PRODUCTION: the database dropped whole under the cleanup protocol (952 rows across 42 tables, simulated then executed, verified by data between and after) and rebuilt by the pipeline's 67 migrations at `0ca8d72`, legacy tables included; the production database password rotated (when, never what) and the connection accepted in the rotated container; the named-exception list empty; sub-step 6 is session 3's, after head deploys
- [gf-rebuild-production-new-registry-2026-09-13.md](gf-rebuild-production-new-registry-2026-09-13.md) — refactor step 9, sub-steps 3 and 4 on PRODUCTION: the new EvidenceRegistry `0xDE42…9823` deployed on Base MAINNET by the researcher's registrar key at 0.0000038 ETH, bytecode identical to the old; the configuration rotated; the in-container read names it with `totalEvidence()` 0; the ledger's `successor` set; the freeze until session 3's head deploy; the registrar's thin wallet
- [gf-rebuild-production-ledger-2026-09-13.md](gf-rebuild-production-ledger-2026-09-13.md) — refactor step 9, sub-step 2 on PRODUCTION: the REGISTRY LEDGER for the old mainnet contract `0x0e21…0d22`, 20 entries explained (12 extraction anchors over `contentHash`, 8 evidence names) and committed as `apps/glass-fortress/backend/registry-ledger/8453-0x0e21….json`, MAINNET, no ruling needed; `successor` null until the rotation
- [gf-rebuild-production-measure-2026-09-13.md](gf-rebuild-production-measure-2026-09-13.md) — refactor step 9, sub-step 1 on PRODUCTION: `0ca8d72` deployed to `master` as the first of two advances (nine additive migrations applied, drift clean); the old mainnet registry read from STATE (20 entries, every one explained by a column), extractor equality 83 of 83, the archive serving 83 of 83 as held; the backup's three records; one tracked page and no calibration data on production, correcting what was carried in
- [gf-thesis-step-19-2026-09-12.md](gf-thesis-step-19-2026-09-12.md) — thesis step 19, framing: the T1 contract green (#420) and the owed staging walk run 2026-09-13 as the first UNSTEERED claude.ai run; ONE framing walked to a CHOSEN claim under `PATIENT_RIGHTS_13` with `MATERIAL_INFORMATION` MISSING and the assessor naming the document, its holder and a date range; the clause "a contradiction fails the substring check and is shown flagged" NOT MET — no contradiction produced, none manufactured; what claude.ai sees (an 80-char line per tool, never the connector's `instructions`); the four PRs the run landed (#426–#429: instructions, a prompt+resource probe, all ten Nuremberg articles + two Israeli provisions each element with its meaning, the surface saying WHEN); and the findings by owner — trajectories inherit the extraction's blindness, the FDA sentence absent from the 0805 derivation, an unchanged stretch has no citable name
- [gf-thesis-step-18-2026-09-11.md](gf-thesis-step-18-2026-09-11.md) — thesis step 18, the schema on the target schema: one hand-written migration of 94 statements, approved by sha and landed byte for byte; the seven rulings and the one REVIEW corrected itself on before it was ruled (the debate's opener is STORED, because every act row A2 defines stores its researcher); nothing is deleted — RESTRICT, never CASCADE and never SET NULL, on all 32 keys of the touched tables; A2's "REQUIRED on X" arms as CHECKs, their Json arms demanding an object because `Prisma.JsonNull` passes `IS NOT NULL`; the precondition MEASURED by one simulated TRUNCATE over a closed set of nine tables, because `db:simulate` is blind to a column drop; the TAKING read back from `pg_constraint` and `information_schema` in the container; one transaction proven VERSION-PINNED in the engine's source, with a guard that no migration carries BEGIN/COMMIT; a guard in a non-gating job holds nothing, so the CHECK cases live in the run that gates; NORMALISE moved to a pure module with the detection pin unchanged and reproduced; and what the step does not claim — no writer until step 20, so no CHECK has yet refused a real row
- [gf-thesis-step-17-2026-09-10.md](gf-thesis-step-17-2026-09-10.md) — thesis step 17, the acceptance suite, failing: a jest project `thesis` of 399 cases in twelve files, every file red and every case that reaches a module red by name until thesis steps 18–24 build it, evidence's predicates CALLED and never re-spelled; landed as PR #413 and corrected by PR #416; the step's lesson — SATISFIABLE IS NOT CONFORMANT: the grading's positive controls were written TO the cases, a post-merge cold read found 2 HIGH and 6 MEDIUM, and after the follow-up's loop a cold conformance review by a fresh seat found 3 MEDIUM more; the one ruling whose GROUND was wrong — "names the diff" rested on built code, and evidence A1 wins; two readiness cases unsatisfiable on their world, invisible behind the loader's red; the A4 amendments now owed; what a builder of steps 18–24 needs from the suite, and the `thesisAssertions` tag step 17 owed and did not give; and twelve questions still the researcher's, the first whether a CALLED `decide_gap` is a paid call
- [gf-evidence-step-15-2026-09-10.md](gf-evidence-step-15-2026-09-10.md) — evidence step 15, the gate: `publishable` and its evidence-half fold, the six checks of A6 as a projection of one evaluation, check 17 rebased onto CURRENT(diff) with its fold unchanged, `audit-theses` keyed on the failure's REASON; the one ruling whose GROUNDING was wrong — the strict fold rested on a dated findings doc for five rounds, and what decides it is refactor plan §4 rule 3 and thesis plan §5 — and the protocol line it produced, a dated doc CORROBORATES and never DECIDES; every predicate the gate composes proven a CALL by breaking it at its own definition, re-run on the landed tree; the returned exit code held by nothing until a process-level test; `tsc` and eslint both blind to the test tree; three questions still the researcher's; and no staging exercise until thesis step 20
- [gf-rulings-audit-2026-09-10.md](gf-rulings-audit-2026-09-10.md) — every implementation-time ruling since corpus PR 3, classified against the four designs: SIX were already decided by a clause and should have reached the researcher as FOUNDs (the debate row that JOINS, `NOT_AUTHOR` on the debate writes, `expectedSequence`, the record's name, `audit-theses` over zero subjects, the ledger entry as thesis 24's); ONE contradicted thesis A4 and was caught by a cold read within hours, formed from a NARRATIVE doc rather than an appendix; everything else EXTENDS. The design-session rulings are out of scope — they made the designs. The one rule not yet in the protocol: a ruling is grounded in an appendix or a plan step, never in a narrative document
- [gf-evidence-step-14-2026-09-09.md](gf-evidence-step-14-2026-09-09.md) — evidence step 14, the review: the six rulings before the sketch and the one the reviewer found in the contract itself (§3c named `pageLog` as the `meta.target` precedent, which reads no `meta` at all); the stop-shaped list with its count first, `cause` as a union and one row per mention, and the decision as ONE transaction that re-pins no citation; the writer scan split by TABLE AND VERB, repairing the per-file gap that would have let a re-pin through, with a coverage case because a verb no row names is unscanned; FOUR instruments that said green about nothing in one loop — the segment rule, the LATEST sequence, the page memoisation and ONE TRANSACTION, the last held by a double that handed its callback the same client; two decoys the compiler refused, one reporting zero tests and one 88 of 121; five stale KEEP tags, one of them a RENAME; and no staging exercise until thesis step 20
- [gf-evidence-step-13-2026-09-09.md](gf-evidence-step-13-2026-09-09.md) — evidence step 13, the debate on a citation: three things the tree could not serve as the appendix reads, ruled before the sketch (the debate's `evidenceId` losing its unique index so a second thesis JOINS the row, `NOT_AUTHOR` from the thesis flows, the assessor as a successor with its Hebrew prompt rewritten); the drift 11b left, found by this step's own pre-condition and fixed first as PR #403, with the schema-half invariant and `LAND`'s new post-condition; the four tools, the passage per paragraph, promotion as one transaction with no chain write; four scans with decoys, one of which caught a real bare transaction on its first run; `argued` proven untested by a stub that passed 1,216 cases, then tested; two KEEP sweeps that reported clean over zero subjects; and no staging exercise until thesis step 20
- [gf-evidence-step-12-2026-09-09.md](gf-evidence-step-12-2026-09-09.md) — evidence step 12, the reads, written the evening after it landed: why the step was pulled forward (a read that answered "nothing has been looked at" out of columns the walk had stopped writing); six rulings before the code — `attributed` from the STORED verdict as `true|false|null` with the anchor-time check extended and one ATTRIBUTED function given an entry-lookup parameter, the record's name on every timeline entry, PUBLIC_PAGE over the current pin with the EVER arm red by name, `NOT_PUBLIC` on the chain check, RECOMPUTABLE / VERIFIED / FLAGGED built early, plain text through the one extractor; the four PUBLIC reads identical for everyone; the acceptance file that had never compiled; the one-symbol scan found blind to generics; and the maintenance re-check that found the corpus holds four anchored captures, not six
- [gf-evidence-step-11b-2026-09-08.md](gf-evidence-step-11b-2026-09-08.md) — evidence step 11b, the build on the emptied ground: the migration and the four statements the offline generator proposed that did not land (four foreign keys SET NULL where the file has RESTRICT); the identity module's test vector derived at a shell and the malformed fixture the module refused when `xxd -r -p` silently truncated an odd-length digest; both instruments observed to FAIL, ten cases by name; the sha256 scan stated as a property, catching a stale entry in its own list on the day it was written; the six rebases onto the record key and the content version, with `DETECTION_VERSION` moved v2 → v3 because which claims are found changed; five verdicts removed rather than left unreachable; and one ratchet REGRESSION removed rather than baselined
- [gf-legacy-switch-2026-09-08.md](gf-legacy-switch-2026-09-08.md) — evidence step 11a, the legacy switch in three deletion PRs, one per layer: what left the tree and why, by tag; the lint debt falling 350 → 146 → CI's by DELETION rather than repair, with the ratchets saying so as `IMPROVEMENT NOT LOCKED IN`; the MCP surface 49 → 13; the retired-names scan's three halves — a tool is a NAME, a module is a RESOLVED IMPORT, a script or route is a SENTENCE a live surface says to a person — each decoy-proven, with one named allow-list entry for the registry ledger; and the two things the deletion exposed, a mock dependency across describe blocks and a sibling-import gap in the scan
- [gf-walk-corrective-pass-2026-09-08.md](gf-walk-corrective-pass-2026-09-08.md) — walla's corrective pass at the connector: the walk asked NOTHING where the same pass had asked about thirty times per redesign, the first live evidence for the judged-capture amendment; `get_diff_input` reads legacy columns the walk stopped writing at step 5 PR 1 and answered "nothing has been looked at" in the shape of a real negative — `DiffContentVersion` has one writer and no reader, so the corpus's own content is invisible to the platform, and evidence steps 11 and 12 were pulled before corpus step 10; a session's MCP tool list can be older than the deployed code; and the pass PROVEN by query — zero chunks on all three diffs, the article never changed — with the walk paying a classifier call on each empty one
- [gf-walk-step-5-rewalk-2026-09-07.md](gf-walk-step-5-rewalk-2026-09-07.md) — refactor step 7 on staging: the ONE re-walk of walla, driven through the connector after every extraction change landed — four texts superseded to v3 with the previous kept and no anchor touched, every spanning diff re-derived; the Q4 attribution defect found live and fixed mid-walk, the one false row recorded; PROVEN FROM THE BYTES that the article never changed and both positional link rules were mutilating one sentence; F1 the page highlighting by string equality and F2 survival checked against the rules' own output; the corrective pass restamped 12-09 and stopped on 12-18 (Gate 2 + Gate 4), where a judged DUPLICATE's removals are re-asked because SEEN excludes it
- [gf-walk-step-5-exercise-2026-09-06.md](gf-walk-step-5-exercise-2026-09-06.md) — refactor step 5 on staging: the walk WRITING, driven through the connector one call at a time — four captures acquired and anchored at registry indexes 0–3 under the scheme, three diffs as pair + content version, every write verified from chain and database state; the article never changed and every diff is rule-induced; survival UNCHECKABLE across rule changes; a third archive answer (a per-capture 429) and the skip widened to an UNFETCHED row on the researcher's word
- [gf-rebuild-staging-drop-and-survey-2026-09-06.md](gf-rebuild-staging-drop-and-survey-2026-09-06.md) — refactor step 9, sub-steps 5 and 6 on staging: the database dropped whole under the cleanup protocol (2,008 rows, 41 tables, simulated then executed) and rebuilt by the pipeline's 64 migrations; the researcher and the connector returned; the three pages re-surveyed, 169 work-list rows, nothing anchored; corona `20250423145731` a known loss; sub-step 6 resumes after step 5
- [gf-rebuild-staging-new-registry-2026-09-06.md](gf-rebuild-staging-new-registry-2026-09-06.md) — refactor step 9, sub-steps 3 and 4 on staging: the new EvidenceRegistry `0xDA3B…4C73` deployed on Base Sepolia by the researcher's registrar key, bytecode identical to the old; the configuration rotated; `get_environment` names it, `totalEvidence()` 0; the freeze until step 5
- [gf-rebuild-staging-ledger-2026-09-06.md](gf-rebuild-staging-ledger-2026-09-06.md) — refactor step 9, sub-step 2 on staging: the REGISTRY LEDGER for the old Sepolia contract, 44 entries explained and committed as `apps/glass-fortress/backend/registry-ledger/84532-0x65b9….json`, TESTNET; the completeness test that holds it
- [gf-rebuild-staging-measure-2026-09-06.md](gf-rebuild-staging-measure-2026-09-06.md) — refactor step 9, sub-step 1 on staging: the old registry read from STATE (44 entries, 7 explained by no column), extractor equality 112 of 112, the archive serving 111 of 112 as held; the freeze rule between rotation and step 5
- [gf-walk-step-6-measurements-part-2-2026-09-06.md](gf-walk-step-6-measurements-part-2-2026-09-06.md) — refactor step 4 on staging, PART 2: Gates 1, 2 and 4 under rules created by MARKING; six stops, none preventable by trust (derived); walla's stops are CSS-hash churn, corona's a permanent Gate 1 alarm read from the code; two findings for the flows doc
- [gf-walk-step-6-marking-verified-2026-09-06.md](gf-walk-step-6-marking-verified-2026-09-06.md) — refactor step 6 on staging: walla and corona each marked in the new page from a stored capture, promoted by `approve_article_rules`, read back three ways; the approval's transaction window found and fixed (PR #360); the page reshaped in three rounds; part 2's opening stops
- [gf-walk-step-4-measurements-2026-09-05.md](gf-walk-step-4-measurements-2026-09-05.md) — refactor step 4 on staging, PART 1: Gate 5's confusion table over twenty labelled diffs (the premise holds on the sample; five empty-input diffs are step 5's finding) and the reporting walk's first stops; part 2 (Gates 1, 2, 4) is its own dated record after step 6
- [gf-walk-step-3-refusals-verified-2026-09-05.md](gf-walk-step-3-refusals-verified-2026-09-05.md) — refactor step 3 on staging: the two registered tools' refusal surface, five calls, nothing written; the write paths wait for steps 5 and 6
- [gf-walk-step-2-survey-verified-2026-09-05.md](gf-walk-step-2-survey-verified-2026-09-05.md) — refactor step 2 on staging: `held` equalled every page's snapshot count; the early cutover decided; one change owed to step 3
- [gf-pre-design-plans-triage-2026-09-04.md](gf-pre-design-plans-triage-2026-09-04.md) — every pre-design open item, one disposition each; six rulings
- [gf-era-detector-thresholds-2026-09-01.md](gf-era-detector-thresholds-2026-09-01.md) — Level 4: the two detectors measured, no threshold adopted
- [gf-level4-mcp-loop-verification-2026-09-01.md](gf-level4-mcp-loop-verification-2026-09-01.md) — Level 4: the MCP loop end to end, a signal that resets
- [gf-level4-third-marking-walk-2026-09-01.md](gf-level4-third-marking-walk-2026-09-01.md) — Level 4: the news page
- [gf-level4-first-marking-walk-2026-08-31.md](gf-level4-first-marking-walk-2026-08-31.md) — Level 4: the page used by a human for the first time
- [gf-level4-second-marking-walk-2026-08-31.md](gf-level4-second-marking-walk-2026-08-31.md) — Level 4: the filtering question
- [gf-anchor-verdict-incident-2026-08-30.md](gf-anchor-verdict-incident-2026-08-30.md) — 105 false verdicts on staging, from a correct fix; repaired
- [gf-candidate-source-measurement-2026-08-30.md](gf-candidate-source-measurement-2026-08-30.md) — Level 6: the free option survives; do not re-measure
- [gf-evidence-input-soundness-2026-08-30.md](gf-evidence-input-soundness-2026-08-30.md) — Levels 6, 9: the summary gate, built and measured
- [gf-level-diagnostics-2026-08-30.md](gf-level-diagnostics-2026-08-30.md) — Levels 7–10: 26 findings, breadth first
- [gf-positive-control-2026-08-30.md](gf-positive-control-2026-08-30.md) — Level 3 clause 1 executed for the first time
- [gf-published-thesis-fda-claim-2026-08-30.md](gf-published-thesis-fda-claim-2026-08-30.md) — Level 9: the published thesis carried a false claim
- [gf-cross-environment-write-postmortem-2026-08-29.md](gf-cross-environment-write-postmortem-2026-08-29.md) — production's database with staging's chain; why scripts run in-container only
- [gf-framing-assessor-defects.md](gf-framing-assessor-defects.md) — 2026-08-26: one stock phrase, three models; answered at thesis flows T1
- [gf-diff-truncation-dev-plan.md](gf-diff-truncation-dev-plan.md) — 2026-08-26: the chunking defect, root cause to repair on both environments
- [gf-staging-data-loss-postmortem-2026-08-21.md](gf-staging-data-loss-postmortem-2026-08-21.md) — the wipe; §5's mitigation predates the pre-deploy migration step

## Superseded — banner in place, kept because something current still cites it

- [gf-researcher-playbook.md](gf-researcher-playbook.md) — the staging transcript, steps 1–40; succeeded by the researcher's day; tests still read it as a fixture source
- [gf-blocked-url-recovery-dev-plan.md](gf-blocked-url-recovery-dev-plan.md) — succeeded by document flows §9
- [gf-trajectory-citation-dev-plan.md](gf-trajectory-citation-dev-plan.md) — succeeded by thesis flows T2
- [gf-thesis-publication-gate-dev-plan.md](gf-thesis-publication-gate-dev-plan.md) — succeeded by thesis flows T5
- [gf-thesis-walk-production-handoff.md](gf-thesis-walk-production-handoff.md) — succeeded by thesis refactor plan step 26
- [gf-production-thesis-replay-plan.md](gf-production-thesis-replay-plan.md) — succeeded by thesis refactor plan step 26
- [production-help-center-build.md](production-help-center-build.md) — succeeded by the in-chat tutorial
- [gf-pending-migration-diff-pair-is-two-captures.md](gf-pending-migration-diff-pair-is-two-captures.md) — succeeded by evidence A2's pair CHECK, ruled 2026-09-04; built at refactor step 11

## Elsewhere in this folder

- `archive/` — obsolete documents, moved with history; each opens with a banner naming where its subject lives now, or that it has none
- [integrity/index.html](integrity/index.html) — the public integrity board, generated by `npm run integrity:board` from `integrity/ledger.json`; not markdown, and un-indexed until 2026-09-21, when the index reader was extended past `.md` and found it
