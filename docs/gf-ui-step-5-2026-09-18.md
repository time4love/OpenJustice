# UI step 5 — THE PUBLIC THESIS PAGE: the researcher's Markdown, the palette, and the voice the archive was wearing

**LANDED IN TWO PARTS.** **PR #513 → `staging` `e7237d0`** (commit `03b2ee0`, 24 files, +900 / −90). CI run
35329721223, both required jobs SUCCESS — `glass-fortress-frontend` 1m03s, `glass-fortress-backend` 3m00s — and
CI's own line, **"Lint debt unchanged: 82 problems across 15 rules."** Frontend deploy `a76c9fca` **SUCCESS**;
backend `6d9720e5` **SKIPPED**, final for the service that did not change. `db:check-drift`:
**"No difference detected."**, printed twice. **A SECOND PR follows it** and is what this document's §12 records:
three defects the researcher found on a PHONE against that deploy, and the font ruling that came out of them.
Base for every measurement below: `staging` **`68ef3c4`**.
Contract: `docs/gf-ui-refactor-plan.md` UI-5 :372–:460 and §10 :1120–:1127; `docs/gf-ui-flows.md` §16–§23, §32, §41;
`docs/gf-ui-design-session-2026-09-16.md` §1.5, §1.8, §3; `docs/gf-thesis-flows.md` T5, A5 :1557–:1580;
`COMPLIANCE.md` :82–:99. The binding image is the design canvas, pages 1–3.
Prior rounds of this step: `docs/gf-ui-step-4b-2026-09-17.md` (the shell it renders inside).

## 1. WHAT THIS STEP DOES NOT DO, FIRST, so nothing green below is read as more than it is

- **It does not settle the page's HEADING.** The `<h1>` is still the published claim, which on the live thesis is
  **558 rendered characters**. The researcher ruled on 2026-09-18 that this is *"not a display problem but a
  specification question about the thesis's title"* and gave it **its own session**. Everything the heading would
  have needed — a short provision form, one backend field, a `§17 :529` amendment, a new string — was taken OUT of
  this step and none of it was built. `git diff 68ef3c4 -- apps/glass-fortress/backend` is **0 lines** for the
  whole round, and `messages/` is **0 lines**: no new string landed.
- **The sticky context line rides with that session**, on the researcher's own reasoning: it is a clamped copy of
  the claim, so its content problem *is* the title problem. Measured: `<p class="sticky-line">`, `top: 0`,
  `z-index: 10`, 29 px, `white-space: nowrap` with an ellipsis, and **not a control** — it cannot be opened. This
  is the third round it has been raised in.
- **No case holds that the palette's tokens reach real elements.** `palette-is-the-system` reads `globals.css` as
  SOURCE; jsdom computes no cascade. The browser readings in §4 are what hold it, and they live here — the seam
  `docs/gf-ui-refactor-plan.md` §6 exists for.
- **No case holds a figure written in a comment.** Three went stale in this round alone; see §9 and issue #512.
- **The deployed pass over chunks 4–7 is still owed**, and is recorded when it runs.

## 2. WHAT LANDED — four chunks, seven grading rounds

| chunk | what |
|---|---|
| 4 (PR #507, before this round) | every Markdown capability in the published TEXT: emphasis restored from six `break;` cases that received and discarded it, `table` and `strikethrough` enabled, twelve table token types mapped — and a heading that had shipped at weight 600 where the board says 700 |
| 5 | **the researcher's Markdown in the other nine prose fields**, one rule, one implementation |
| 6 | **the palette**: paper, sidebar, muted and line |
| 7 | **the archive leaves the researcher's voice**, and keeps its serif by declaring it — plus three stale measurements the palette had left behind |

**Numbers at close**, re-derived by REVIEW rather than taken from a report: frontend **28 suites / 193 / 0** (from
26 / 176 at the base: +2 suites, +17 cases) · `tsc` 0 · lint 0 errors / 4 warnings, all four pre-existing in
`src/app/[locale]/theses/page.tsx` · build 0 · messages **1,504 / 1,504**, difference 0. Backend `tsc` 0 ·
**177 / 3,011 / 0** · diff **0 lines**. MCP surface **45**. Migrations **72**.

## 3. THE MARKDOWN RENDERER AND ITS SUBSET — `docs/gf-ui-refactor-plan.md` :427's answer

:427 requires this step's dated doc to record **which** renderer and **which subset**. Read from
`src/lib/markdownToReact.tsx`, not remembered.

**The renderer: `markdown-it`, used as a PARSER and never as an HTML producer.** Its token stream is mapped to
React elements here, so a citation chip is a React element where its token stands and **nothing is ever handed to
`dangerouslySetInnerHTML`**. Configuration: the `commonmark` preset with `html: false`, `linkify: false`,
`typographer: false`, then `.enable(['table', 'strikethrough'])` — the same preset, never `'default'`, which would
switch `linkify` back on.

**THE SUBSET THAT RENDERS**

| | |
|---|---|
| blocks | paragraph · headings · bullet and ordered lists · list item · blockquote · **table** (`table · thead · tbody · tr · th · td`, each an open/close pair — twelve token types) · fenced and indented code · horizontal rule |
| inline | text · **strong** · **em** · **strikethrough** · inline code · autolink · soft break · hard break |
| headings are DEMOTED | `h1`→`h2`, `h2`→`h2`, `h3`→`h3`, `h4`/`h5`/`h6`→`h4`. The page owns `h1`; the researcher's structure starts below it |
| a table | gets a scrollport of its own (`md-table-scroll`), so "one column" governs the reading MEASURE and a table never widens the page (§6 :963) |
| alignment | honoured as `md-align-left/right/center` classes — the parser's `style="text-align:…"` is never carried through |
| bidi | a URL, a date in either form, a 64-hex hash and a 14-digit archive timestamp are each wrapped in `<bdi dir="ltr">`, inside a table cell exactly as in a paragraph |

**WHAT DOES NOT RENDER, AND WHY EACH ONE IS A DECISION**

- **Raw HTML** — `html: false`. "Every capability" never meant markup; HTML in the researcher's text renders as
  the text it is.
- **Linkify** — off, so a bare URL stays isolated text rather than becoming a link nobody chose. A CommonMark
  autolink, `<https://…>`, is a link the researcher wrote and does render.
- **Task lists and footnotes** — ruled OUT by the researcher, 2026-09-18, and written into §17 :538. Both need a
  plugin, and a thesis is an argument, not a working document. Their marks stay in the text as written.
- **An image renders its ALT TEXT and no `<img>`** — so nothing in the researcher's Markdown causes a public page
  to fetch an external resource.
- **A token type the map does not know renders as its own source text**, never silently dropped.
- **A citation token renders as NOTHING in the nine prose fields.** §17 :538 gives chips to THE TEXT, which
  resolves them against the body's mentions; these fields carry no resolved citations, so a token could only reach
  a reader as its own `0x…` name — an id shown as text, which §4 :167 forbids. It is cased.

**THE RUNTIME** is unchanged from the step's original brief: the pages render on the server, the share metadata is
composed from the body (§32 :822–:823), and TipTap is not used anywhere.

## 4. THE PALETTE — and the reason it is TWO changes and not one

The researcher, reading the deployed page: *"the centre's background looks too brown … I don't want to lighten it
to white, but to lighten this cream to the shade Claude Code has, which is our reference."*

**The reference was SAMPLED, not assumed** — claude.ai read in a browser: page background `#FCFCFB`, `--bg-100`
`#F9F9F7`. Warmth as red-minus-blue: our paper **9**, our sidebar **15**, the reference **1**.

| token | was | is | why |
|---|---|---|---|
| `--paper` | `#FAF7F1` | **`#FCFCFB`** | warmth 9 → 1 |
| `--paper-deep` | `#F3EEE4` | **`#F2F1EE`** | 15 → 4; the sidebar, not the centre, was the browniest surface |
| `--ink-muted` | `#6B6157` | **`#4E463F`** | contrast 5.66 → **9.00** on the paper, 5.23 → **8.18** on the sidebar |
| `--line` | `#E4DCCF` | **`#E6E5E2`** | 21 → 4, by `globals.css` :10's own rule — a border carries no meaning and may not carry a hue that says nothing |

**THE TWO HALVES OF THE COMPLAINT WERE TWO DIFFERENT AXES, AND THIS IS THE SECTION'S POINT.** Every paper between
the old value and the new moves the muted's contrast by **0.23**. The readability the researcher asked for is
carried *entirely* by the text colour. **No element failed WCAG AA before the change** — the tightest were 5.23 and
5.66, passing AA and failing AAA at 12–14 px — so lightening the paper alone would have reported a fix over an
unchanged reading. That is `the axis checked was the axis thought of`, which this step paid for four times.

**Measured on the real body afterwards**, off real elements at both widths: `body` `rgb(252,252,251)` ·
`ASIDE.shell-sidebar` `rgb(242,241,238)` · a real border `rgb(230,229,226)` · muted-on-paper **9.00**,
muted-on-sidebar **8.18** · warmth 1 / 4 / 4 · `scrollWidth` 375 at phone and 1440 at width. **The 12 px
disclaimer the researcher named as hard to read now measures 9.00, from 5.66.**

Ink, surface and the four meaning colours did not move, and all four still carry on the new paper: olive 5.85 ·
gold 3.04 · amber 3.55 · seal-red 6.48.

**One consequence, recorded and the researcher's:** the new line on the new sidebar reads **1.115 : 1**, where the
old pairing read 1.204. Two borders are really drawn there (`SPAN.shell-foot`, `SPAN.flex`). Neither value is a
WCAG failure — a decorative divider carries no text requirement — and on every other surface the new line is fine
(1.227 on paper, 1.260 on the white cards). Nobody measured that axis until the palette had already been ruled.

## 5. THE ARCHIVE WAS WEARING THE RESEARCHER'S VOICE

`RecordPane` rendered a captured page's text, a diff chunk and a trajectory's claim text through
`ResearcherWords` — the component whose marker `[data-researcher-words]` means *"the researcher wrote this"* and
whose selector applies their serif. **Those three values are the ministry's words, held in the archive.**

It is why the Markdown rule could not live in `ResearcherWords`: putting it there would have parsed the three
values §16 :521 forbids parsing, and a `#` or `*` standing in a captured page would re-interpret what the record
says — an evidence change wearing a display change's clothes.

So the rule went into a new component, `ResearcherProse`, and the archive left the marker. **The face did not
change and that was the requirement**: `.record-captured` now declares `font-family: var(--font-serif)` itself, so
it is a stated decision about the record rather than a side effect of the wrong voice's marker. Measured before
and after on the real body: `frankHebrew` · 15 px · 25.5 px · `pre-line` · `max-height: 420px` ·
`overflow-y: auto` · 4,539 characters — identical, with `clientHeight` 418 against `scrollHeight` 6021, so §18
:569's *"long content scrolls"* is intact and the 91 %-locked evidence of an earlier round has not returned.
`[data-researcher-words]` now marks **2** blocks on the thesis page where it marked 3, both the researcher's.

## 6. THE INSTRUMENTS, AND THE DECOYS BOTH SEATS PLANTED

Three new instruments, each observed RED BY NAME before its code existed, each re-proved by a decoy REVIEW planted
itself and restored by sha256 comparison with the porcelain count equal before and after.

- **`researchers-markdown`** (11 cases) — every prose block renders BLOCKS not one run-on text node, on both
  pages; the rationale renders one `<p>` per paragraph AND its emphasis becomes `<strong>`/`<em>`; an appeal field
  renders its paragraphs with its label beside it; **the archive is never parsed**; the trigger holds the rendered
  blocks and **no interactive descendant**; the clamp is a multi-line paragraph **by value**; `data-intake` marks
  the block container; a token reaches the reader as neither a chip nor its `0x…` name.
- **`palette-is-the-system`** (6 cases) — the four moved tokens by value; everything else byte-identical; **no old
  value survives anywhere in the token block**; the muted clears AAA on both surfaces with a two-sided floor; the
  four meaning colours still carry; the warmth is gone, in red-minus-blue.
- **`built-as-drawn` gained a ninth property** — `font-family` by value, which it had never asserted.

**THE THREE LAX PROBES ARE THE RECORD'S REAL EVIDENCE**, because a broken-only decoy proves only that a suite
runs. A hand-rolled `text.split(/\n\n/).map(p => <p>)` — the right block count, no Markdown — reddens the
rationale case. A muted of `#4A4A4A`, neutral and comfortably past AAA but the WRONG value, reddens both the
by-value case and the contrast floor. A `.record-captured` face DECLARED but switched to the sans reddens F1.
Each of the three would have passed an instrument that asserted a property NAME instead of a VALUE.

**A DECOY THAT REDDENED NOTHING FOUND A REAL GAP, not an unexercised region.** Deleting the newly declared
`font-family` reddened **0** — because `built-as-drawn` F1 asserted eight named properties and `font-family` was
not among them. The face this step had just turned into "a stated decision" was held by no case at all, before or
after. *"A decoy that reddens nothing has found an unexercised region"* is the default reading, not the only one.

## 7. THE READING TEST — RUN BY THE RESEARCHER, AND **IT PASSES**

The researcher, verbatim: **„הקולות השונים ברורים אבל יש עדיין בעיות"** — followed by eight numbered items.

**This reverses UI-4b's answer** (*"how can a reader tell the researcher's words from the platform's marks from a
labelled opinion? Currently it is not clear"*). `docs/gf-ui-flows.md` §41 :961–:962's three-voices question is
answered in the affirmative for the first time, and the eight items that followed were **not** about the voices —
they were layout, copy, behaviour and content, i.e. what a reader notices once the voices stop getting in the way.

**And the mechanism that works is not the one the contract describes.** The voices read apart even though the
body text carried **no** `[data-researcher-words]` marker at all — 0 of 5 paragraphs. The separation a reader
perceives is carried by TYPOGRAPHY (serif at 17/1.75 for the argument, sans pills for the platform's dated ticks),
not by the component split §16 :517–:521 names. The contract's mechanism and the working mechanism are different
ones, and that is worth knowing before either is changed.

### RUN A SECOND TIME, against option C, and it PASSES AGAIN — with a DIFFERENT mechanism

**The researcher, verbatim:** *„הקולות ברורים, הטקסט שלי נבדל מהסימנים. הפלטפורמה מדברת בפונט יותר קטן המוקף
בקופסא או באמצעות תגיות טקסט (כמו התאריכים עם הנקודה)"*

Asked SEPARATELY from their approval of the look (*"This version looks very good"*), which is recorded as a
judgement of the visual and was deliberately NOT accepted as the answer to §41 — they are different questions, and
option C had removed the mechanism the first answer rested on.

**This corrects the paragraph above rather than repeating it.** Option C makes everything the reader sees Heebo
except the archive, so the typeface difference is gone — **and the test still passes.** The researcher names what
actually carries it: **SIZE and ENCLOSURE** — the platform speaks in a smaller font, inside a box or a tag.
So the earlier diagnosis was INCOMPLETE rather than wrong: the pills were always doing part of the work and the
serif was carrying the rest; remove the serif and the pills carry it alone.

**Three readings of this question now exist: UI-4b FAILED · UI-5 under A PASSED by typography · UI-5 under C
PASSES by size and enclosure.** What a later change must protect is therefore NOT the typeface: it is that the
platform's marks stay SMALLER and ENCLOSED. A mark grown to the size of the prose, or stripped of its pill, would
cost the property where the whole font change did not — and no case holds that today.

## 8. THE DEFECT THE FIXTURES COULD NOT SEE, AND WHY

The researcher asked whether „נימוקי הפרסום" appeared as one block because the text had no formatting or because
the page dropped it. **Measured, and it was the page.** The live rationale is 2,632 characters with **six
newlines over three blank-line breaks**, rendering with `childElementCount: 0` under `white-space: normal` — four
paragraphs collapsed into one run. The body, by contrast, rendered 8 child elements through `markdownToReact`,
whose only caller was `ThesisText.tsx`.

**Why 176 green cases never saw it:** `published.json`'s `rationale` was a single 70-character sentence with no
newlines. A fixture with nothing to flatten cannot witness flattening. The instrument now carries a **two-sided
floor** — every prose fixture field must state more than one paragraph, asserted and printed BEFORE the rendering
is — because the defect was one-sided and a floor demanding "at least one paragraph" would have been met by the
very fixture that hid the bug. This is the third time this shape has been paid for.

## 9. WHAT ROTTED SILENTLY, AND THE THING NOTHING HOLDS

The palette moved four tokens, and **two sentences quoting derived figures became false without anything
failing**: a contrast ratio in `globals.css`'s `.letter-fill` note, and the same ratio plus a greyscale-step count
in `LetterDialog.tsx`. All three were correct when written, of the old paper and the old line. Re-measured:
1.074 → **1.118** against the paper, and 26 → **23** greyscale steps for the underline.

`built-as-drawn` and `palette-is-the-system` hold declared values in CSS. **Nothing holds a figure quoted in a
comment**, and only a hand-audit found these. Filed as **#512**.

**It also moves a ruling's ground.** The researcher ruled „א+ג" — the `--letter-fill` surface AND a `--line`
underline on the same span — *because* the fill works by hue alone and the underline is what a greyscale reader
still sees. That underline is now 23 steps at 1 px rather than 26. The ruling was not re-opened here.

## 10. DECLARED, AND THE AMENDMENTS THIS STEP WROTE

Three in-place amendments, all REVIEW's with the seat declared, all shown to the researcher before writing, all at
**zero line delta** with every hunk a single line replaced by a single line — so nothing below any edit moved and
every in-repo `:line` citation still resolves. `gf-ui-flows.md` **1111 → 1111**; `gf-ui-design-session-2026-09-16.md`
**124 → 124**.

- **`gf-ui-flows.md` §16 :521** — the researcher's voice renders their Markdown wherever it appears; it reaches
  their words alone and never the archive's bytes; the folded preface shows a short clamped paragraph, and its
  trigger may hold the rendered blocks but never an interactive descendant.
- **`gf-ui-design-session-2026-09-16.md` :42** — the palette, with the reference sampled and the two axes stated.
- **`gf-ui-design-session-2026-09-16.md` :42** — `--letter-fill` named in a design for the first time: a SURFACE
  and not a meaning, on `--door-field`'s precedent, with the reason the „א+ג" ruling has two cues written in.

**Files declared out of KEEP by this step, each with its measured size:** `RecordPane.tsx` (116) and
`LetterDialog.tsx` (203 → 210, comments only). Everything else on UI-5's KEEP list is `git diff` **0**, verified
per chunk from a file with a subject count and a PLANTED positive control.

## 11. STILL OPEN, AND WHAT COMES NEXT

**Issues filed 2026-09-18:** **#508** the UI-5 leftovers (an orphaned copy key, a stale case title, seven unstyled
`.record*` classes, one dead component, one marker held by a single case) · **#509** the corporate registry name in
checked-in files · **#510** a title on `ThesisVersion` · **#511** the `mcpRoutes` socket flake · **#512** a scan for
measurements quoted in comments.

**The researcher's, recorded rather than ruled here:** the sidebar divider at 1.115 : 1 · the „א+ג" ruling against
23 greyscale steps · the no-English `public/icon_foia.png`, which `public/` waits for at diff 0 · Q2, the third
right-pane tab for a previous version, which §10 :1124 names, board 3A does not draw and §19 :595 is silent on ·
and A5's plan-versus-image disagreement, where the plan's prose says "24/22" and every canvas board, phone boards
included, draws 24.

**The heading and the sticky context line go to their own specification session**, by the researcher's ruling of
2026-09-18. **UI-6 — the door — begins only once this step closes.**

## 12. AFTER IT LANDED — three defects a READER found on a phone, and the font ruling

Nothing below was visible to the 193 green cases that shipped in PR #513. All three were found by the researcher
holding the deployed page on a phone, which is the reading `docs/gf-ui-refactor-plan.md` §6 exists to require.

**1 · The researcher's Markdown still rendered as ONE BLOCK — and the parser was innocent.** Their question was
the diagnosis: *"if md is formatted, how does it not also generate the paragraphs?"* **It did.** `markdown-it`
produced four `<p>`; what it cannot do is say how far apart they sit. Measured rather than assumed: a `<p>` inside
a bare iframe computes a **16px** margin, and inside this app it computes **0** — the reset removes the browser's
own spacing, so rendered Markdown has none unless a rule restores it. The rule had been restored at exactly ONE
call site, as `ThesisText`'s `space-y-3` utility, which the nine prose fields never passed through. **Gaps 0,0,0
→ 12,12,12**; the body text unmoved. The rule now belongs to the prose (`.md-prose > * + *`). **Why no case saw
it:** every case in the file COUNTS BLOCKS — and counting blocks is the parser's question while separation is
CSS's. The new case is a SOURCE scan, because jsdom computes no cascade and a rendered gap there is 0 either way;
its decoy declares `margin-top: 0`, which would satisfy "the rule exists" and change nothing a reader sees.

**2 · THE FONT — measured first, then ruled.** The researcher reported that most of the page was "the less pretty
font". Measured by which face actually DRAWS — canvas width against the declared stack, against Times, against
sans — **everything was correct**: Frank Ruhl Libre on the body, Heebo on the chrome, nothing falling back. What
they had described was a PREFERENCE against §1.8, not a defect. Three options were rendered with the published
text and **the researcher chose C**, then **„א"** for the archive. Five sites moved to `--font-sans`; two keep the
serif, which is NOT retired — that would return R57's F8 — but narrowed to **A DOCUMENT BROUGHT VERBATIM THAT THE
PLATFORM DID NOT AUTHOR**: `.record-captured` and `.letter-body`. Measured after: claim, body, headings, rationale
and the small note all `heeboHebrew`; the archive `frankHebrew`; every one proved not a fallback.

**3 · The phone drawer would not close.** Reproduction, the researcher's: thesis → tap a date → the archive
full-screen → back-swipe → **home** → open the drawer → pick the thesis → *"I cannot close the menu"*. Measured:
`Shell.tsx` reads no route at all, and the drawer had three ways out — Escape, the scrim, the toggle — of which
following a link inside it was not one. The body stays `overflow: hidden` while it is open, so the page behind
cannot scroll and the drawer covers its own toggle: that is the "cannot close". **The same-route tap is the hard
case** — the drawer lists what was opened in this browser, so the entry chosen is usually the page already open
and `pathname` never changes. Proven at 390 px: `sameRoute: true`, `aria-expanded` still `true` after the tap.

**FOUND, and owed a step rather than a patch:** opening a record pushes **no history entry** (`history.length`
unchanged), while the design makes that record full-screen at phone width — so a back-swipe does not close the
record, it leaves the thesis. Whether the pane becomes addressable or only history-aware is a design question for
the shell's owner step.

### THE INSTRUMENTS CAUGHT THREE THINGS, AND ALL THREE WERE REVIEW'S OWN

- **A route effect, written first and removed.** `test/shell.test.tsx` asserts *"nothing under components/shell
  READS A ROUTE"* — its assertion names two specifiers while its TITLE names the property, so a `usePathname`
  passed it **while making that title false**. The click handler alone was kept and the shell stayed route-free.
- **R57's F8 case failed the moment option C was applied**, exactly as designed — it asserts the serif is applied
  and that the READING REGION is one of the places. Re-pointed as a DECLARED edit with its authority named, title
  changed together with the assertion, and its second clause re-pointed rather than dropped: `applied > 0` alone
  is the vacuity F8 was found inside.
- **REVIEW's own drawer case failed on its own negative control.** It clicked "a button inside the drawer" to
  prove the handler's scoping — and the first button there is the COLLAPSE control, which unmounts the whole
  `Sidebar` and every link with it. The next assertion read an empty subject set and `requireSubjects` refused it.
  Split into two cases, with the links asserted FIRST on an untouched drawer.

### AND `tsc` CAUGHT WHAT A GREEN SUITE COULD NOT

`fontsAreLocal.test.ts` called `.sort()` on `requireSubjects`' **readonly** array. The suite was green — jest
transpiles the types away — and `npx tsc --noEmit` exited **2**. It is also a lesson about this seat's method:
several checks ran `npx tsc --noEmit | head -3` and then echoed "tsc clean" UNCONDITIONALLY. It never lied,
because the output was empty each time, but **an echo that does not read the exit code is not a check** — the same
shape as `${PIPESTATUS[0]}`, which returns empty in zsh and did so again here.
